# 参与贡献

欢迎提交 Bug、素材包和功能改进。先在 Issue 中说明较大改动，便于确认与现有玩法的关系。

## 本地开发

需要 Node.js 20.12 或更新版本，无需安装 npm 依赖。

```bash
npm start
npm test
npm run check
```

`dist/` 存放实际运行源码和素材，请勿按普通构建缓存删除。

提交前检查核心流程，包括购买、家具调整、旅行、存档导出与恢复。修改打包逻辑后运行 `npm run package`，确认 ZIP 中的资源引用齐全。

## 提交问题

请提供设备、浏览器版本、复现步骤、预期与实际结果。截图和脱敏后的错误信息有助于定位；请勿提交 API Key、`.env` 或含私人照片的存档。

## 素材与文档

新增素材请遵循 [素材包规范](docs/ASSET-PACK-SPEC.md)，注明来源和许可，并运行资源检查。现有 Q90 WebP 保留原尺寸和透明通道，避免对成品反复有损压缩。

修改 README 或规范时，同步 `dist/docs/` 中的对应副本；`npm run check` 会检查要求保持一致的文档。示例图片、地图和代码有各自的授权范围，请保留对应许可文件。
