# 旅行照片风格预览

20张图片沿用现有风格样例，原始PNG字节未修改。仅供设置页展示，不加入素材包 assets、行程或生图请求。

实际使用的文字来自原始 prompts.json 的 style 字段，并去掉固定雪山、湖泊、落日等构图限制；从未使用原始完整重绘 Prompt。文字目录在 app/core/photo-styles.js，前端图片路径在 app/photo-style-previews.js。
